<% if current_user == @user %>
  <% provide(:title, "マイページ") %>
  <%= render 'mypage', user: @user, events: @events %>
<% else %>
  <% provide(:title, "#{@user.name}さん") %>
  <%= render 'other_user', user: @user, events: @events %>
<% end %>
